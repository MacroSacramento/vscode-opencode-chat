// ── State ────────────────────────────────────────────────────────────────
// Shared webview state: a single mutable object so every module reads and
// writes the same instance. DOM refs start `null` and are filled in init().

export const state = {
  connected: false,
  sessions: [],
  activeSessionId: null,
  busy: false,
  stopping: false,
  stoppedStream: false,
  loading: true,
  pendingAssistantId: null,
  pendingUserEl: null,
  catalog: null,
  agent: null,
  model: null,
  usage: null,
  subagents: [],
  subagentsVisible: false,
  pendingPermission: null,
  pendingQuestion: null,
  // Per-pane state maps (keyed by sessionId). The multi-pane grid routes
  // every session-scoped message to its pane; these maps hold the per-session
  // values so one pane's stream/busy/pending state can't leak into another's.
  paneBusy: {},
  paneStopping: {},
  paneStoppedStream: {},
  panePendingAssistantId: {},
  panePendingUserEl: {},
  paneAgent: {},
  paneModel: {},
  paneUsage: {},
  paneSubagents: {},
  panePendingPermission: {},
  panePendingQuestion: {},
  // DOM refs (filled in init)
  app: null,
  disconnected: null,
  conversation: null,
  sessionList: null,
  sessionCount: null,
  sessionToggle: null,
  progress: null,
  emptyNoSessions: null,
  emptyConversation: null,
  input: null,
  sendBtn: null,
  newSessionBtn: null,
  retryBtn: null,
  emptyNewBtn: null,
  toast: null,
  agentPickerBtn: null,
  agentBadgeValue: null,
  modelPickerBtn: null,
  modelBadgeValue: null,
  thinkingToggle: null,
  thinkingToggleValue: null,
  contextUsageLine: null,
  showThinking: false,
  subagentsToggle: null,
  permissionCard: null,
  questionCard: null,
  slashPopup: null,
  atPopup: null,
  files: null,
  agentMenu: null,
  modelMenu: null,
  helpOverlay: null,
  helpList: null,
};
